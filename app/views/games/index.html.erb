<ul>
  <%= content_tag_for(:li, @games) do |game| %>
      <span>Game <%= game.id %></span>
      <%= link_to 'Show', game, id: dom_id(game, 'show') %>
  <% end %>
</ul>

<br/>

<%= paginate @games %>

<br/>

<%= link_to 'New Practice Game', new_game_path, id: 'new_game' %>

